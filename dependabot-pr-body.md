# Dependabot config PR

This PR adds a basic Dependabot configuration to enable weekly non-security updates for npm direct dependencies.

- Schedules updates weekly
- Limits open Dependabot PRs to 5
- Targets root directory ("/")

If you'd like changes (include devDependencies, monorepo directories, or different frequency), I can update the config and push a new commit.
